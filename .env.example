# Application
NODE_ENV=development
PORT=3001
API_PREFIX=api/v1

# Database (MySQL)
DATABASE_URL="mysql://hrms_user:hrms_password@localhost:3306/hrms_db"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# Platform JWT (for platform admin portal)
PLATFORM_JWT_SECRET=your-platform-jwt-secret-here
PLATFORM_JWT_EXPIRES_IN=8h

# File Storage — 'local' (disk, no external service needed) or 's3' (MinIO/S3-compatible)
STORAGE_DRIVER=local
STORAGE_LOCAL_DIR=uploads

# MinIO (only used when STORAGE_DRIVER=s3)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=hrms-files

# SMS Gateway (Fast2SMS) — only required in production; OTPs are logged to console in development
FAST2SMS_API_KEY=

# Firebase Cloud Messaging (Push Notifications)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Throttling
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# App URL (used for onboarding links)
APP_URL=http://localhost:3001
